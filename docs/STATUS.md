# Project status

**Last updated:** 2026-09-09
**Current milestone:** C25 — Visualization and performance audit

## Complete

- A0–A11 research handoff under `research/`.
- Research archive status: ready for normalization with unresolved claims preserved; it is a first-pass handoff, not a proof-standard completed genealogy.
- Research coverage includes 104 canonical genealogically relevant identities after aliasing, 87 proposed source/source-set entries, 53 accepted/probable/contextual/unresolved places, 9 excluded or unattached places, and 12 movement entries.
- C0 application foundation: Next.js, React, TypeScript, Tailwind CSS, shadcn configuration, Base UI, and Lucide.
- C0 validation: install, lint, type-check, production build, and zero-vulnerability npm audit passed.
- C1 durable architecture, product, design, data, visualization, methodology, status, and decision documents.
- C2 research-ingestion audit covering all people, branches, relationships, event families, places, sources, confidence states, identity risks, contradictions, incomplete records, and geographic uncertainty.
- C3 canonical TypeScript contracts for people, relationships, events, places, sources, historical dates, confidence, research status, stable IDs, aliases, and the single family graph.
- C3 dependency-free runtime validation for dates, IDs, evidence requirements, aliases, cross-references, migration endpoints, and accepted-lineage safeguards.
- C3 Node test foundation with seven passing schema and reference-integrity tests.
- C4 canonical graph with exactly seven people, eight supported in-scope relationships, 26 evidence-backed events, 10 places, and 15 normalized sources.
- C4 referential-integrity coverage for authorized people, exact relationship edges, source resolution, and all event/person/place/relationship references; the full suite now has 13 passing tests.
- C5 branch-by-branch normalization of 50 sufficiently established deeper direct ancestors: 28 maternal and 22 paternal.
- C5 canonical graph totals: 57 people, 82 relationships (56 parent-child, 23 spouse, 3 partner), 120 events, 38 places, and 53 sources.
- C5 data-quality audit for orphan IDs, invalid references, self relationships, duplicate IDs, likely duplicate identities, source coverage, chronology, impossible parent chronology, circular ancestry, and contradictory claims.
- C5 audit result: 0 errors and 3 expected warnings for explicitly retained, non-overlapping birth alternatives. All 50 deeper ancestors have an accepted path to Michael Buquet.
- C5 validation: 18 passing tests plus successful type-check, lint, and production build.
- C6 graph-injected query API for people, direct and recursive relationships, shortest paths to Michael, branch membership, source-backed surname matching, place associations, uncertainty-aware alive-in-year results, events, and source support.
- C6 preserves complete relationship/path evidence, weakest-edge confidence, historical-date overlap, place-association roles, source-reference routes, and indeterminate temporal outcomes.
- C6 validation: 28 passing tests plus successful type-check, lint, and production build.
- C7 centralized Nordic minimal/editorial tokens for warm canvas and surfaces, graphite/cool-neutral typography, fjord-blue interaction, confidence treatments, compact radii and controls, focus, responsive whitespace, and reduced motion.
- C7 portable Inter Variable font, refined Base UI-backed shadcn button, shadcn-style badge, Lucide conventions, and a limited responsive foundation specimen on the placeholder route.
- C7 validation: 28 passing tests plus successful type-check, lint, production build, and responsive visual inspection.
- C8 persistent root application shell with skip navigation, Family Atlas brand link, Explore/People/Places/Stories/Research primary navigation, active-route semantics, and a clearly deferred global-search affordance.
- C8 intentional responsive navigation: inline desktop navigation at `lg` and a Base UI-backed modal sheet for phone and tablet widths, including focus management and explicit close behavior.
- C8 validation: 28 passing tests plus successful type-check, lint, production build, and browser checks at 320px, 390px, 768px, 1366px, and 1920px with no page-level horizontal overflow.
- C9 framework-independent Explore state contracts and pure reducer for `activeView`, `selectedPerson`, `selectedYear`, `selectedBranch`, `selectedPlace`, and `evidenceMode`.
- C9 React provider and focused hooks mounted in the persistent application shell; view transitions preserve selections, routes do not own live visualization state, and D3 remains outside the state layer.
- C9 state semantics distinguish clearing filters from a full reset, validate selected years against the canonical 1–9999 bounds, and preserve object identity for no-op transitions.
- C9 validation: 35 passing tests plus successful type-check and lint.
- C10 responsive, accessible Family Atlas home page with the requested headline, supporting concept, and primary action in a restrained editorial/museum composition.
- C10 People, Places, and Time introduction resolves its Michael/parent, Carencro/Dulac/Spring Grove, and verified birth-event previews directly from the canonical application graph; no statistics or genealogy were invented.
- C10 validation: 35 passing tests plus successful lint, type-check, production build, and browser checks from 320px through 1920px with no horizontal overflow or runtime-console issues.
- C11 Family Tree V1 derives disposable ancestry hierarchies from the canonical graph for all-family, paternal, maternal, and selected-person scopes; every connector retains its source relationship ID, endpoints, and confidence.
- C11 uses `d3-hierarchy` for layout, `d3-shape` for connectors, and `d3-zoom` with its `d3-selection` binding for pan/zoom navigation. React continues to own SVG marks, controls, shared selection, expansion, and accessible semantics.
- C11 initially reveals three generations, supports node-level keyboard expansion, Show all/Fold older controls, selected-person rerooting, zoom controls, reset, and shared `selectedPerson` updates.
- C11 provides a keyboard-operable SVG tree plus an equivalent structured list, preserves known date uncertainty, omits unknown dates, uses non-color confidence cues, and disables zoom animation for reduced motion.
- C11 validation: 42 passing tests plus successful lint, type-check, production build, and browser interaction checks at 320px, 390px, 768px, 1366px, and 1920px with no page overflow or runtime-console issues.
- C12 responsive person details: an accessible shadcn/Base UI Sheet on desktop and bottom Drawer on mobile, opened from either the SVG tree or its structured-list alternative.
- C12 person presentation derives name, relationship label and path, supported lifespan, event-backed biography, places, direct family, confidence, and de-duplicated source count from the canonical graph. Unknown dates and missing biographical events remain absent or explicitly unknown; retained date conflicts remain visible.
- C12 actions preserve shared Explore context. Show in tree reroots the existing tree; journey and timeline actions set the future shared view selection without inventing an unavailable visualization.
- C12 relationship paths are calculated by the framework-independent graph utilities and presented Michael-first (for example, Michael → Paulette → Rita); no path is hard-coded in the component.
- C12 keeps person confidence separate from weakest-edge path confidence, preventing a verified identity from visually upgrading a probable relationship chain.
- C12 validation: 47 passing tests plus successful lint, type-check, production build, and live interaction checks at 320px and 1366px for Drawer/Sheet switching, relationship rendering, progressive event disclosure, shared-view actions, selected-person rerooting, and Escape dismissal.
- C13 reusable `/people/[personId]` profiles for every accepted canonical person plus an editorial `/people` index. All 57 stable-ID routes are statically generated, and unknown IDs return the standard not-found response.
- C13 profiles include Story, Family, Life in places, Timeline, Records & evidence, and Open research questions sections. Rich and sparse records use the same structure without synthesized dates, places, relationships, or narrative filler.
- C13 links parents, spouses/partners, children, related event participants, place associations, events, relationships, and source support using canonical person paths or stable-ID anchors. The C12 Sheet/Drawer now links its full-biography action to the corresponding profile.
- C13 open-research presentation is limited to explicit normalized uncertainty: probable/unresolved relationships and events, unknown event dates, unresolved alternate names, retained source contradictions, and relevant record notes. It never scrapes `research/open-questions.md` at runtime or infers missing branches as questions.
- C13 validation: 50 passing tests plus successful type-check, lint, and production build of the People index and all 57 statically generated person routes. Live checks covered rich and sparse profiles at 320px and 1366px, stable-ID deep links, active People navigation, and the Sheet-to-profile handoff without runtime warnings.
- C14 Timeline V1 derives person rows, temporal extents, supported lifespan intervals, and axis ticks from the canonical graph for all-family, maternal, paternal, and selected-person scopes.
- C14 uses `d3-scale`, `d3-time`, `d3-axis`, and `d3-time-format` for UTC temporal geometry and ticks while React owns SVG/HTML rendering, scope, selection, and accessible interaction.
- C14 preserves date semantics: exact dates are points; years and ranges retain bounded extents; circa dates use dashed tolerance intervals; before/after values remain open-ended; unknown dates are excluded from the axis and disclosed separately.
- C14 lifespan rows require both bounded birth and death evidence. Conflicting alternatives expand the possible extent and retain the narrower common supported interval rather than selecting an invented date.
- C14 connects Tree and Timeline through shared Explore state, provides a horizontally scrollable readable canvas on narrow screens, keyboard-selectable person rows, and an equivalent structured record list.
- C14 validation: 55 passing tests plus successful type-check, lint, and production build. Live browser checks at 320px and 1366px covered view switching, all/maternal/selected scopes, keyboard person selection, cross-view person/branch persistence, local horizontal timeline scrolling without page overflow, and a clean runtime console.
- C15 shared Time Navigator reads and writes the centralized Explore `selectedYear`; Timeline and the future Journeys view have no local duplicate time state.
- C15 uses D3 scale calculations and an isolated `d3-brush` binding for responsive pointer/touch selection. Keyboard arrows, Page Up/Page Down, Home/End, Escape/Delete, and the explicit Clear year action operate the same shared value without animation.
- C15 adds framework-independent event filtering plus map and person-emphasis projections. Supported, possible, indeterminate, excluded, and unfiltered states remain distinct; only conclusively out-of-year people are dimmed.
- C15 Timeline integration renders a bounded calendar-year band, de-emphasizes out-of-year events, preserves incomplete life histories, and retains the year when switching between Tree and Timeline.
- C15 validation: 61 passing tests plus successful lint, type-check, and production build. Live checks at 320px and 1366px covered keyboard and pointer selection, cross-view year persistence, keyboard/button clearing, responsive sizing without page overflow, and a clean runtime console.
- C16 Journeys Map V1 derives supported locations and movements from accepted canonical events for all-family, maternal, paternal, and selected-person scopes. Shared `selectedPerson`, `selectedBranch`, `selectedYear`, and `selectedPlace` remain React-owned Explore context.
- C16 uses `d3-geo` for a Natural Earth projection and boundary paths, `d3-shape` for explicitly schematic movement curves, and isolated `d3-zoom` behavior for pan/zoom. React renders every SVG/HTML mark and accessible control.
- C16 distinguishes documented, probable, and mixed-location anchors without inventing coordinates. Named sites lacking inspected coordinates use disclosed town/county anchors; historical Acadia and Port Royal remain explicit but unplotted because no modern equivalent was normalized.
- C16 encodes documented movements with solid schematic curves and strongly inferred movements with dashed curves. Separate known locations with unknown routes use paired endpoint rings and are structurally prohibited from producing a connecting path.
- C16 vendors published Natural Earth 1:110m Admin 0 and Admin 1 GeoJSON boundary context under `public/data/`, with source and version disclosed in the interface and visualization documentation.
- C16 validation: 68 passing tests plus successful lint, type-check, and production build. Live browser checks at 320px and 1366px covered published-boundary loading, pointer and keyboard marker selection, collision-safe responsive clusters, zoom detail, branch/year filtering, selected-person handoff, and zero page-level horizontal overflow.
- C17 People directory projects all 57 accepted canonical people into restrained, linked typographic rows and supports composable branch, recorded-surname, generation, explicit-birthplace, and person-confidence filters.
- C17 filter semantics retain evidence boundaries: generation is parent-child depth from Michael; surname values come only from canonical or alternate recorded names; birthplace requires an accepted birth event with an explicit canonical place reference; and unavailable filter values are not manufactured.
- C17 validation: 75 passing tests plus successful lint, type-check, and production build. Live browser checks at 320px and 1366px covered all 57 stable-ID profile links, five-filter composition, filter clearing, profile navigation, uncertainty labels, and zero page-level horizontal overflow.
- C18 Places index groups all 38 accepted canonical locations into restrained geographic lists and links each one to a statically generated stable-ID detail route.
- C18 place profiles derive associated people, recorded surname forms, events, historical-date spans, supporting sources, canonical place hierarchy, and all three movement classifications. Broader parish, county, and region profiles include only normalized child-place associations and label them as occurring within that geography.
- C18 connects place profiles to person profiles, supporting event/source anchors, and shared Journeys state. Person profiles, the home-page place preview, and Journeys map details now link back to canonical place routes.
- C18 validation: 83 passing tests plus successful lint, type-check, and production build. Live browser checks at 320px and 1366px covered all 38 place links, parish-level precision, historical geography without a manufactured modern country, Places-to-Journeys selection, stable cross-links, and zero page-level horizontal overflow.
- C19 global Command search projects accepted canonical people, places, recorded surname forms, and normalized sources through a framework-independent index and ranking utility. Search includes supported alternate names, historical place names, source metadata, citation handles, and stable IDs without reading research prose.
- C19 routes person and place results to their stable profiles, surname results to a shareable People-directory filter, and source results to 53 statically generated normalized source-record routes. The persistent header opens search by button or Command/Control+K; shadcn Command provides arrow-key selection and Enter navigation inside an accessible Base UI dialog.
- C19 validation: 92 passing tests plus successful lint, type-check, and production build of 153 static pages. Live checks covered alternate-surname search, a surname deep link, keyboard-opened source search, source navigation, place search at 320px, and zero page-level horizontal overflow.
- C20 Research section exposes the archive methodology, accepted normalized source inventory, complete verified/probable canonical conclusion registers, explicit unresolved claims and reviewed open questions, plus archive-versus-application coverage boundaries.
- C20 global Evidence Mode uses the existing centralized Explore state across routes. Probable and unresolved confidence remains visible in normal browsing; enabling Evidence Mode adds restrained `● Verified` marks and supporting confidence context across people, places, profiles, tree, timeline, journeys, and research surfaces.
- C20 keeps the archive boundary intact: `src/data/research.ts` is a reviewed application-facing question/methodology projection, `src/lib/genealogy/research-overview.ts` derives claim and source registers from the canonical graph, and no runtime code reads research Markdown.
- C20 validation: 96 passing tests plus successful lint, type-check, and production build of 154 static pages. Live browser checks covered global mode persistence, quiet/default versus enabled verified marks, the Research information hierarchy at 1366px and 320px, keyboard-accessible controls, and zero page-level horizontal overflow.
- C20A separates claim provenance from research confidence with optional, source-linked `family-confirmed` and `documented` support. Provenance can coexist on a claim and is runtime-validated against both the claim's source references and source evidence classes.
- C20A marks Michael's six explicitly supplied parent/grandparent parent-child edges as family-confirmed and verified. The Verna→Aubin and Rita→Paulette edges retain documentary evidence and carry both provenance kinds; spouse claims, parentage subtypes, biographical details, and distant ancestry are unchanged.
- C20A Evidence Mode now reveals classified provenance on immediate-family rows, relationship paths, and Research conclusion entries while normal browsing stays quiet. Four formerly probable close-family edges now render as verified, so their paths and tree connectors no longer inherit a probable classification.
- C20A validation: 100 passing tests plus successful lint, type-check, production build of 154 static pages, and `git diff --check`.
- C20B source review found no sibling list in the three accessible original Family History chats. Michael then directly identified Sidney Paul Roger, Edmond Paul Buquet, and Gina Buquet Nevils in the current task, resolving the close-family identity and relationship gap.
- C20B adds the three siblings through five verified, family-confirmed parent-child edges: Sidney through Paulette; Edmond and Gina through Aubin and Paulette. Siblinghood remains query-derived, Sidney's unnamed father is not invented, and Gina's previously probable identity match is now both family-confirmed and document-supported.
- C20B applies living-person privacy defaults: no exact sibling birth date is normalized, Edmond and Gina retain only their confirmed 1987 and 1990 birth years, and Gina's tentative middle name remains unresolved. Canonical totals are now 60 people, 87 relationships (61 parent-child, 23 spouse, 3 partner), 122 events, 38 places, and 54 sources.
- C20B validation: 105 passing tests plus successful type-check, lint, production build of 158 static pages, and `git diff --check`. Coverage includes shared-parent sibling derivation, relationship paths, person profiles, directory inclusion, search, Evidence Mode provenance, referential integrity, and living-person date minimization.
- Immediate-family extension adds Karla Vannessa Contreras-Buquet as Michael's verified, family-confirmed wife and Chloé Eloise Buquet and Jolie Renee Buquet as their verified, family-confirmed daughters. Karla's maiden name is retained, and her paternal and maternal ancestry is explicitly deferred.
- Living-person privacy remains year-only: Karla 1989, Chloé 2019, and Jolie 2022. Canonical totals are now 63 people, 92 relationships (65 parent-child, 24 spouse, 3 partner), 125 events, 38 places, and 55 sources.
- Immediate-family extension validation: 108 passing tests plus successful type-check, lint, production build of 162 static pages, and `git diff --check`. Coverage includes spouse/parent-child structure, relationship paths, directory generations, profiles, search, provenance, and living-person date minimization.
- Family Tree V2 makes Chloé and Jolie the focal generation in the default Family scope, with Michael and Karla, Michael's three supported siblings, and Michael's accepted ancestry visible through a graph-derived layout projection.
- Family Tree V2 draws only accepted canonical parent-child and spouse connectors. Sidney remains connected only through Paulette; siblings remain shared-parent derivations; Aubin and Paulette do not receive an inferred spouse edge; and Karla's parents remain absent pending future research.
- The established Michael-centered paternal, maternal, and selected-person ancestry scopes remain available. The Tree's visual focal generation does not change relationship-path, branch, or directory-generation reference semantics.
- The corrected two-sided layout places Karla to the left of Chloé and Jolie and Michael to their right. Michael's ancestry grows outward to the right; supported Karla ancestry will automatically mirror outward to the left when normalized.
- Michael is vertically interleaved with Edmond, Gina, and Sidney and aligned to the daughters' midpoint, balancing the right-hand family group around Chloé and Jolie.
- Family Tree V2 validation: 111 passing tests plus successful type-check, lint, production build of 162 static pages, `git diff --check`, and responsive browser checks at 320px and 1366px with no page-level horizontal overflow.
- C20C adds Aubin's two obituary-identified siblings, Michael Buquet and Cathy B. McRae, and Paulette's four obituary-identified siblings, Russell J. “Rooster” Comeaux, Allen Paul Comeaux Jr., Peggy C. Miller, and Priscilla C. Babineaux/LeBlanc. Aunt/uncle status is derived from shared parent-child edges; no direct lateral-kinship edge exists.
- Direct obituary parent links are verified/documented. The archive's indirectly supported Edmond and Allen Sr. parent links remain probable/documented. No C20C link is labeled family-confirmed, and no living-person birth date, address, contact detail, or public-record-aggregator detail is added.
- Supported spouse reports add Monica, Clarence Miller, Karlon, and Tippy LeBlanc as sparse probable people. Priscilla's two reported spouse relationships remain separate and unordered; neither is used to infer Dexter Babineaux's father. Paternal uncle Michael has no invented spouse or descendant structure.
- The graph now explicitly distinguishes the two Michael Buquet identities, derives parental collateral branch membership, and includes the six aunts/uncles plus supported partners in the daughter-centered Tree projection. Canonical totals are now 73 people, 108 relationships (77 parent-child, 28 spouse, 3 partner), 125 events, 38 places, and 56 sources.
- C20C validation: 119 passing tests plus successful type-check, lint, production build of 173 static pages, `git diff --check`, and live browser checks of the Tree, People directory, stable person profiles, keyboard search navigation, and Evidence Mode. Coverage includes obituary provenance, parent confidence, shared-parent sibling derivation, branch membership, paths to Michael, Tree connectors, People directory generations, search, sparse profiles, Evidence Mode source support, explicit same-name non-equivalence, and referential/data-quality integrity.
- C20D adds nine obituary-identified first cousins through accepted parent-child structure: Paige Bartholomew and Sean “Rusty” McRae on the paternal side; Conrad Miller, Rustie Lynn Comeaux, Rhyan Comeaux, Dexter Babineaux, Gerard Comeaux, Casey Comeaux, and Brandi Comeaux on the maternal side. No arbitrary cousin relationship type was added.
- The obituary-level grandchild identities are verified/documented, while all nine aunt/uncle→cousin parent assignments remain probable/documentary-inferred because the available archive does not preserve an inspectable direct citation for those exact edges. Richard Russell McRae is retained as Paige and Sean's documented father; Cathy→Paige and Cathy→Sean remain probable and are not upgraded from public-record association evidence.
- C20D preserves `Sean McRae`/`Sean “Rusty” McRae`, records `Ryan Comeaux` and `Ryan Earl Comeaux` as unresolved source forms for Rhyan Comeaux, and resolves `Sid Roger` only as an alternate form for existing sibling Sidney Paul Roger. Lauren Dugas and Collin Adkisson remain excluded from cousin normalization pending separate family-position confirmation.
- The query layer now derives first cousins through the four-edge parent→grandparent→aunt/uncle→child path, propagates weakest-edge confidence, and assigns collateral descendants to Michael's maternal or paternal branch without changing direct-ancestor semantics. The daughter-centered Tree now projects accepted cousins and supported co-parents from canonical edges.
- C20D canonical totals are 83 people, 119 relationships (88 parent-child, 28 spouse, 3 partner), 125 events, 38 places, and 59 sources. C20D validation: 125 passing tests plus successful type-check, lint, production build of 186 static pages, `git diff --check`, and live checks of the Tree, People directory, stable profiles, command search, and Evidence Mode with no browser errors. Coverage includes first-cousin derivation, path evidence, branch filtering, directory generations, stable search aliases, sparse profiles, Tree connectors, evidence presentation, IDs, and referential/data-quality integrity.
- C20E audits the complete lateral graph without adding people or relationship edges. It confirms 2 paternal aunts/uncles, 4 maternal aunts/uncles, and exactly 9 first-cousin candidates, with every aunt/uncle→cousin edge still probable/documentary-inferred and every family-confirmed immediate-family edge preserved.
- C20E corrects sibling branch presentation: Sidney derives as maternal through the shared Paulette edge, while Edmond “Bud” and Gina derive as both through their supported Aubin and Paulette edges. UI labels now describe shared recorded parents without asserting biological/adoptive/legal subtype, and aunt/uncle labels derive from the same shared-parent graph.
- People directory rows now distinguish relationship-path confidence from person confidence. Probable cousin paths remain visibly probable even when the cousin identity itself is verified; verified path evidence stays quiet until Evidence Mode.
- The C20E privacy regression found no public exact dates, addresses, phone numbers, emails, or contact details to remove from the audited lateral records. Regression coverage prevents sparse cousin data from generating Timeline lifespans or Journeys movements and confirms that the public-record association source exposes no URL or citation handle.
- C20E validation: 131 passing tests plus successful type-check, lint, production build of 186 static pages, `git diff --check`, and live browser checks covering Tree and ancestor-focused scope, branch-filtered People results, sibling/cousin labels, probable path presentation, stable profile and search routes, Evidence Mode, sparse Timeline/Journeys behavior, and a clean browser console. The current data-quality audit remains valid with 0 errors, 3 expected historical birth-conflict warnings, and 0 informational findings.
- C21 Patterns V1 adds three archive-bounded aggregate views: 56 unique accepted ancestor identities by supported generation and branch, birthplace-evidence coverage across all 83 accepted people, and verified/probable/unresolved mix within each accepted entity type.
- C21 deliberately omits surname rankings, birthplace/geographic distributions, and branch-completeness percentages because recorded name forms overlap, only 11 accepted people currently have supported birthplaces across 9 canonical places, geographic precision is mixed, and no defensible complete-biological-family denominator exists.
- C21 uses a framework-independent canonical-graph adapter and D3 linear scales for bar geometry while React renders accessible labeled HTML. Missing birthplace evidence remains explicit as 32 people with birth evidence but no established place and 40 people without an accepted birth event; neither group is converted to zero or silently dropped.
- C21 validation: 136 passing tests plus successful type-check, lint, production build of 186 static pages, `git diff --check`, and live browser checks at 320px and 1366px. The four-view Explore switcher now wraps at narrow widths; the Patterns page has no horizontal overflow and produced no browser console warnings or errors.
- C22 completes the cross-view integration audit. Timeline rows and Journeys person controls now open the shared responsive person details surface; person actions use atomic reducer handoffs; Tree focus reliably enters selected-person ancestry scope; and place-detail actions clear incompatible person/branch filters while retaining shared year and Evidence Mode context.
- C22 fixes stale place identity in grouped map anchors: a multi-place cluster now clears `selectedPlace`, and a selected place outside the active scope/year is disclosed rather than silently replaced. Relationship paths, profile routes, and visualization selections continue to resolve one canonical person ID.
- C22 regression coverage exercises person, place, branch, year, and Evidence Mode handoffs; stable route/path identity; grouped-anchor selection; and canonical-graph immutability across Tree, Timeline, Journeys, and Patterns projections. Live browser checks covered Tree → Timeline, Timeline → Journeys, both person-detail entry points, branch/year/evidence persistence, place-detail → Journeys, stable profile routing, desktop Sheet, mobile Drawer, and zero page-level overflow at 390px.
- C22 also declares the existing smooth-scroll behavior on the root document so Next route transitions no longer emit a framework warning; the existing reduced-motion override remains authoritative.
- C22 validation: 142 passing tests plus successful type-check, lint, `git diff --check`, and a clean post-reload browser console.
- C23 completes responsive and accessibility inspection at 320px, 390px, 768px, 1366px, and 1920px across the shell, Explore views, directories, profiles, Research, search, and responsive person details. Tested routes have no page-level horizontal overflow; Timeline retains intentional local horizontal scrolling at narrow widths.
- C23 raises phone/tablet buttons, selects, and disclosure summaries to a 44px minimum target; adds visible focus to Timeline SVG rows; restores focus after person Sheet/Drawer and Command-search dismissal; and adds an evidence-honest Journeys empty state for scopes without mapped locations.
- C23 adds route-specific document titles and an intentional unpublished Stories state so primary navigation does not lead to an unlabeled 404. It also documents the structured alternatives and unavoidable spatial-visualization limitations in `docs/accessibility-audit.md`.
- C23 validation: 142 passing tests plus successful type-check, lint, production build of 187 static pages, `git diff --check`, responsive browser inspection, keyboard interaction checks, and token contrast verification.
- C24 compares the complete accepted graph with the research archive, normalized sources, family-intake records, and Evidence Mode semantics. It adds no people or relationship edges.
- Michael's C24 confirmation upgrades 21 existing close-family parent-child roles: 12 edges forming the two parental sibling groups and 9 aunt/uncle→first-cousin edges. Each is now verified and family-confirmed while retaining its earlier documentary provenance and direct/indirect evidence notes. Parentage subtype and unrelated facts remain unchanged.
- All 2 paternal aunts/uncles, 4 maternal aunts/uncles, and 9 first cousins continue to derive structurally. No direct lateral-kinship edge exists; Sidney remains a sibling rather than a cousin. Lauren Dugas and Collin Adkisson have no canonical people, relationships, or placement and are not planned additions.
- C24 repairs three incorrect research-reference paths for the Jules Comeaux/Joesette R. Comeaux family claims, removes the now-resolved cousin-parent confirmation question from application-facing research data, and records Michael's confirmation in a dedicated family-intake source.
- Privacy review found no sensitive canonical field requiring removal. Living-person dates remain year-only where intentionally public, and the audited lateral people have no normalized exact birth dates, private addresses, contact details, or unsupported Journeys paths.
- C24 validation: 143 passing tests plus successful type-check, lint, production build of 188 static pages, `git diff --check`, and a genealogy audit result of 0 errors, 3 expected historical birth-conflict warnings, and 0 informational findings.
- C25 profiles the 83-person production visualization pipeline and retains React-rendered SVG: the default Tree has 359 SVG descendants, Timeline has 875, Journeys has 233 paths, typed Tree/Timeline/Journey model passes remain below 1 ms on average, and full published-boundary map reprojection averages about 12 ms on the development machine.
- C25 dynamically loads non-default Explore views, reducing the initial page route chunk from 304,936 to 261,212 bytes (14.3%). Shared container measurement and zoom hooks coalesce high-frequency updates to one React commit per animation frame, while memoized Tree and map mark layers avoid reconciling stable SVG descendants during transform-only interactions.
- C25 preserves the ownership boundary: React owns state, marks, and transforms; D3 owns hierarchy, scale, projection, path calculations, and isolated zoom/brush sampling. No Canvas conversion was warranted. Production browser checks at 1280px and 320px covered all views, deferred chunks, zoom, hydration/runtime logs, and zero page-level horizontal overflow.

## Deliberately not started

- Additional collateral, descendant, contextual, unattached, research-only candidate, or rejected identities beyond the currently accepted graph.
- Additional normalization not authorized by the branch coverage audits or later evidence review.
- Standalone Family and published Stories features; the Stories route is currently an explicit unpublished-content state.
- Secondary product routes.
- Hosting, authentication, analytics, editing, collaboration, or synchronization.

## Current repository boundary

The site contains a validated application shell, global canonical-data search, an editorial home/Explore route with daughter-centered Family Tree V2, Timeline V1, Journeys Map V1, Patterns V1, their shared Time Navigator, and responsive person details, plus filterable People, evidence-bounded Places, and Research sections with stable-ID profiles and records. C4 added the source-traceable seven-person foundation; C5 extended that graph with accepted direct maternal and paternal ancestry; C6 added read-only graph utilities; C7 added visual tokens and primitives; C8 added persistent navigation; C9 added shared Explore state; C10 added the canonical-data-backed home page; C11 added the first derived visualization; C12 added person-centered disclosure over that visualization; C13 added person profiles; C14 added the uncertainty-preserving cross-person timeline; C15 made year context operational across temporal views; C16 added the evidence-limited Journeys map; C17 made the People index an evidence-bounded directory; C18 added the canonical Places index and detail views; C19 added global search and stable normalized-source routes; C20 added the evidence-focused Research route and global Evidence Mode; C20A added independent family-confirmed/documented claim provenance; C20B added three privacy-safe, family-confirmed sibling records; the immediate-family extension added Michael's spouse and daughters; C20C added Michael's known maternal and paternal aunts/uncles; C20D added the nine currently identified first cousins while preserving the documentary uncertainty then available; C20E audited and regression-protected that lateral graph; C21 added defensible aggregate patterns over accepted records; C22 hardened cross-view identity and selection handoffs; C23 completed the responsive/accessibility hardening pass; C24 added Michael's later relationship-role confirmation without upgrading unrelated facts; C25 reduced initial Explore delivery and high-frequency visualization churn without changing the React/D3 boundary; and Family Tree V2 centers the daughters without changing canonical-reference semantics. The Stories route intentionally states that no standalone narratives are published yet. `research/manifest.json` records the state at the end of A11, so its `websiteCodeCreated: false` field is historically correct for that research handoff even though C0 subsequently created application code.

No application code may parse `research/` at runtime. The ingestion audit is recorded in `docs/research-ingestion-report.md`; it found 13 reused claim IDs, including 8 semantic collisions. Normalized data uses globally unique application IDs and preserves packet IDs only as file-scoped research provenance. The C5 integrity results are recorded in `docs/data-quality-report.md`; the C6 public query contract is recorded in `docs/genealogy-queries.md`. Further genealogy expansion must remain explicit and reviewed; unresolved claims may not be resolved merely to make the product complete.

## Known environment notes

- `npm run build` uses Next.js's supported Webpack builder because Turbopack's CSS worker cannot bind its internal port in the managed Codex environment.
- npm reports two warnings from unrelated user-level configuration keys; project install and validation are unaffected.
- The repository is connected to `https://github.com/spac3boy/familyatlas.git`; task changes remain unstaged unless the user commits them.
